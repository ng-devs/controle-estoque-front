import { ApplyMaterialThemeDirective } from './apply-material-theme.directive';

describe('ApplyMaterialThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyMaterialThemeDirective();
    expect(directive).toBeTruthy();
  });
});
